import 'package:flutter/material.dart';

class MartScreen extends StatelessWidget {
  const MartScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("MartScreen"),
      ),
    );
  }
}